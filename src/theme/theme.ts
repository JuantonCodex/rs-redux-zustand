import { buttonVariants as button, bagdeVariants as badge } from "./partials";
const defaultStyles = {
  borderRadius: "rounded-md",
  padding: "py-2 px-4",
  fontSize: "text-sm",
  fontWeight: "font-medium",
};

export const theme = {
  defaultStyles,
  variants: {
    button,
    badge,
  },
};
